const add_form_btn = document.querySelector(".add-form");
const remove_form_btn = document.querySelector(".remove-form");

function addForm() {
  const msg = "addForm imported.";
  return msg;
}

export default addForm;
